/**
 * 任务要求:
 *     1. 实现对 Employee 数组的快速排序，排序规则: 优先按 Employee 的名字排序，如果名字相同，则按工资排序
 *     2. 实现对 Employee 数组的快速排序，排序规则: 优先按 Employee 的名字排序，如果名字相同，则按工资排序
 *     3. 也可以实现其他的排序方法，例如堆排序，希尔排序等
 */
public class Sort {
    // [1] 快速排序
    public static void quickSort(Employee[] employees) {

    }

    // [2] 选择排序
    public static void selectionSort(Employee[] employees) {

    }
}

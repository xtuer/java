jdbc:
  driverClassName:        com.mysql.jdbc.Driver    a     # 前后空格会被自动去掉，注释不会被读取
  url: jdbc:mysql://localhost:3306/test?useUnicode=true&characterEncoding=UTF-8
  username: root
  password: 密码 #支持中文
urls:
  - http://127.0.0.1
  - http://localhost
